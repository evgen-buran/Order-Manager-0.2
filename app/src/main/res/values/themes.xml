<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.OrderManager02" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="actionOverflowMenuStyle">@style/MyOverflowMenuStyle</item>
    </style>
    <style name="MyOverflowMenuStyle" parent="Widget.AppCompat.PopupMenu.Overflow">
        <!-- Change the color of the overflow menu icon -->
        <item name="android:tint">@color/white</item>
        <!-- Change the color of the overflow menu text -->
        <item name="android:actionOverflowButtonStyle">@style/MyOverflowMenuTextAppearance</item>
    </style>

    <style name="MyOverflowMenuTextAppearance" parent="android:Widget.Material.Light.ActionButton.Overflow">
        <!-- Change the color of the overflow menu text -->
        <item name="android:src">@drawable/baseline_more_vert_24</item>
    </style>
</resources>